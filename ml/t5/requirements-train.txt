# Phase 2 (fine-tuning) deps. Install on Colab; NOT needed for the backend.
# The local repo venv (Python 3.14, CPU-only) is missing datasets/accelerate/
# sentencepiece, which is why training runs on Colab's free T4 instead.
transformers
datasets
accelerate
sentencepiece   # belt-and-suspenders; flan-t5's fast tokenizer doesn't require it
torch
